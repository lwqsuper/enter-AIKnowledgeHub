I will implement the Digital Avatar "Hua Rujin" with a floating interface and AI interaction capabilities.

### 1. Backend Service (`src/services/chatService.ts`)
- **System Prompt**: Embed the content from `数字分身提示词.md` as the core persona definition.
- **LLM Integration**: 
  - Create a structured `sendMessage` method.
  - **Mock Implementation**: Implement a keyword-based response system (matching terms like "ERP", "JSON", "Learning Path") to return pre-written responses in "Hua Rujin's" style immediately.
  - **API Placeholder**: detailed comments and code structure for future LLM API integration (request format, key handling).

### 2. UI Component (`src/components/DigitalAvatar.tsx`)
- **Visual Design**: 
  - A "Q-style Tech" floating square avatar in the bottom-right corner.
  - Smooth transition animations for opening/closing.
- **Chat Interface**:
  - Chat bubble window with a clean, modern design.
  - Markdown support for rendering structured responses (lists, bold text).
  - Configurable "Welcome Message" displayed on first open.

### 3. Global Layout (`src/components/Layout.tsx`)
- Create a unified `Layout` component that wraps the application content.
- Include `Header`, `Footer`, `Toaster`, and the new `DigitalAvatar` component.
- This ensures the avatar persists across all pages navigation.

### 4. Page Integration
- Refactor existing pages (`Index`, `NewsList`, `NewsDetail`, `AILearningPath`) to use the new `Layout` component, removing duplicate Header/Footer references.

This approach ensures a seamless addition of the feature with zero disruption to existing functionality, while laying the groundwork for real AI model integration.