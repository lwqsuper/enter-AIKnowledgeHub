Based on the project structure (Vite + React + TypeScript) and `package.json`, here is the plan to start the project:

1.  **Install Dependencies**: Run `pnpm install` to install all required packages listed in `package.json`.
2.  **Start Development Server**: Run `pnpm run dev` to start the local development server.
3.  **Preview**: Once the server is running, I will provide the preview URL so you can view the application in your browser.
